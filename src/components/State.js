const userList = [" tergeltengis", "Dashnyam", " dash"];
export default function App() {
  const variable = 0;
  consdt[(index, setIndex)] = useState(0);
  console.log(index);
  return (
    <div className="text-center">
      <button onClick={() => setIndex(index + 1)}>nemh</button>
      <p>{index}</p>
      <button onClick={() => setIndex(index - 1)}>hasah</button>
    </div>
  );
}
